# ── API Framework ──────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.46.0

# ── Data validation & configuration ────────────
pydantic==2.11.10
pydantic-settings==2.10.1

# ── Database & auth (Supabase) ─────────────────
supabase==2.30.0

# ── HTTP & file handling ───────────────────────
python-multipart==0.0.26
httpx==0.28.1
aiofiles==24.1.0

# ── Image processing ───────────────────────────
Pillow==12.2.0

# ── LLM APIs ───────────────────────────────────
openai==2.32.0
anthropic==0.102.0

# ── Environnement ──────────────────────────────
python-dotenv==1.2.2

# ── Tests ──────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0

# ── OCR local (OPTIONNEL — Phase 2, OCR_BACKEND=trocr uniquement) ──
# Decommenter pour installer (~2-4 Go) :
# torch==2.12.0
# transformers==5.8.1
# Installation CPU uniquement (plus leger) :
# pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
# pip install transformers==5.8.1
